<%- include('../partials/header') -%>

<% if(typeof errors != 'undefined'){ %>
<ul>
<% for(var i = 0; i < errors.length; i++) { %>
    <div class="alert alert-success" role="alert">
        <li> <%= errors[i].msg %></li>
      </div>
<% } %>
</ul>
<% } %>


<div class="col-6 offset-2">
    <form class="card" action="/usuario/validaLogin" method="POST">
        <div class="card-header">
            <h2 class="card-title mb-0">Entrar no sistema</h2>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        <label>Email</label>
                        <input class="form-control" name="email" type="email" placeholder="example@example.com" />
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        <label>Senha</label>
                        <input class="form-control" name="password" type="password" placeholder="Digite sua senha..." />
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer d-flex justify-content-between text-right">
            <div>
                é novo por aqui? 
                <a href="/usuario/cadastro"><b>Cadastre-se</b></a>
            </div>
            <div>
                Mater logado
                <input type="checkbox" name="logado">
            </div>
            <button class="btn btn-primary">Enviar</button>
        </div>
    </form>
</div>

<%- include('../partials/footer') -%>